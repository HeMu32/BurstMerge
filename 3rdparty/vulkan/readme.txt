This folder is reserved for a local Vulkan SDK installation.

Place the following content here for MinGW builds:
- Include/  (Vulkan headers)
- Lib/      (Vulkan import library like vulkan-1.lib)

Do not commit SDK headers or libraries into git. Only this readme and
.gitignore should be kept under version control to document the expected
manual dependency layout.
