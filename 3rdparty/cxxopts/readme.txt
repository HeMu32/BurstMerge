This directory is reserved for the cxxopts header-only library.

Place the following content here:
- include/  (cxxopts header files)

Do not commit the full source
or other library files into git. Only this readme is intended
for version control to document the dependency layout.
